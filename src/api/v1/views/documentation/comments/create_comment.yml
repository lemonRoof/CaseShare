  Create a new comment into the database
  ---
  tags:
    - Comments
  parameters:
    - name: x-token
      in: header
      schema:
        type: string
        format: jwt
        required: true
    - name: data
      in: body
      schema:
        $ref: '#definitions/info'

  definitions:
    info:
      properties:
        content:
        type: string
        required: true

  responses:
    201:
      description: A new comment is saved to the database and object is returned
    400:
      description: The data you are sending is not a JSON format
    404:
      description: The user is invalid
    403:
      description: The token is invalid