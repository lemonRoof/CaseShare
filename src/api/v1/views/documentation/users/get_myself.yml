  get a user's own profile
  ---
  tags:
    - Users
  parameters:
    - name: x-token
      in: header
      schema:
        type: string
        format: jwt
        required: true

  responses:
    200:
      description: Users successfully returned
    